# Advanced Python, Fall 2026

**Repo: `Advanced-Python-Fall-2026`**

Live hub: `https://silvermanjonathan.github.io/Advanced-Python-Fall-2026/advanced_python_hub.html`

Day page pattern: `https://silvermanjonathan.github.io/Advanced-Python-Fall-2026/wed01_cold_read.html`

Raw file pattern: `https://raw.githubusercontent.com/silvermanjonathan/Advanced-Python-Fall-2026/main/<filename>`

Robofun, 110 West End Avenue. Wednesdays 4:00 to 5:30, 16 September to 16 December 2026.
Thirteen sessions, grades 5 to 8.

Flat repo. All CSS and JS inline. No `index.html`. `advanced_python_hub.html` is the
homepage.

## Deploy

Create the repo as `Advanced-Python-Fall-2026` under `silvermanjonathan`, push the root of
this folder to it, and set GitHub Pages to serve from the branch root. Do not add an
`index.html`. The hub is the homepage and every day page links back to it.

If you are embedding in Google Sites, use a full-page URL embed per page. No page uses
`localStorage` or `sessionStorage`, so the embed will not break.

Re-upload `advanced_python_hub.html` whenever you add or rename a page. It holds the only
copy of the syllabus link table.

## The thirteen sessions

| # | Date | Page | Core |
|---|---|---|---|
| 1 | 16 Sep | `wed01_cold_read.html` | Cold read 60 lines, then a naming and docstring pass |
| 2 | 23 Sep | `wed02_return_and_modules.html` | `return`, composition, your own importable module |
| 3 | 30 Sep | `wed03_counting.html` | Dicts, sets, `Counter`, break a Caesar by frequency |
| 4 | 7 Oct | `wed04_messy_files.html` | Dirty file in, clean data and a reject log out |
| 5 | 14 Oct | `wed05_transposition.html` | Rail fence, then a route cipher with a signed key |
| 6 | 21 Oct | `wed06_hashing.html` | The seal collision, then `sha256` and the avalanche |
| 7 | 28 Oct | `wed07_what_it_costs.html` | Linear vs binary, brute force, a timed runtime table |
| 8 | 4 Nov | `wed08_heuristics.html` | Hill climbing, a genetic algorithm, honest comparison |
| 9 | 18 Nov | `wed09_monte_carlo.html` | Monty Hall, convergence, and a silent bug. Capstone brief |
| 10 | 25 Nov | `wed10_benford.html` | Benford on two CSVs, histogram in pygame. Sketches due |
| 11 | 2 Dec | `wed11_markov.html` | Word-pair model, then the honest LLM conversation |
| 12 | 9 Dec | `wed12_classes.html` | `class`, `__init__`, `self`, a list of objects, the plume |
| 13 | 16 Dec | `wed13_orbits_demo.html` | Gravity toward a body, time steps, parent demo |

No class on 11 November, Veterans Day. Fourteen Wednesdays fall in the term and that is
the one that comes out.

**Two dates to confirm against Robofun's closure list before this goes to parents.**
30 September falls in the intermediate days of Sukkot. 25 November is the day before
Thanksgiving. Session 10 is built self-contained for the attendance risk: nothing later
in the term depends on it.

## Programs

Put these on the classroom machines. Four data files are needed on disk, and all four are
generated by scripts here, so nothing has to be fetched over the network.

| Session | Files |
|---|---|
| 1 | `sweep_report.py` |
| 2 | `sweep_tools.py`, `sweep_report2.py` |
| 3 | `caesar_crack.py` |
| 4 | `clean_readings.py` (writes and reads `readings_raw.txt`) |
| 5 | `transposition.py` |
| 6 | `avalanche.py` |
| 7 | `what_it_costs.py` |
| 8 | `smarter_than_brute.py` |
| 9 | `monty_hall.py` |
| 10 | `make_data.py`, `benford.py`, `honest_ledger.csv`, `cooked_ledger.csv` |
| 11 | `markov.py`, `corpus.txt` |
| 12 | `plume.py`, `verify_plume.py` |
| 13 | `orbit.py`, `verify_orbit.py` |

Sessions 1 to 9 and 11 are terminal only and run anywhere. Sessions 10, 12, and 13 need
pygame. Confirm pygame is installed before 25 November.

Seeds are fixed so classroom runs match the pages: 4703 in session 8, 1963 in session 9,
20261125 in session 10, 20261202 in session 11, 20261209 in session 12.

## Verification

Every output printed on a page came from running the program, on Python 3.12.3 with
pygame 2.6.1. The two graphics sessions were verified headless.

```
cd build
python3 make.py        # writes all 14 pages, then validates
python3 crosscheck.py  # runs every program, asserts each quoted line appears
```

`make.py` checks tag balance, reveal and answer parity, escaped angle brackets in `<pre>`
blocks, link resolution, dash characters and entities, and absence of browser storage.
`crosscheck.py` runs each program and asserts that every line quoted on its page appears
in the real stdout, including the broken Monty Hall variant.

Also verified in a headless browser: no horizontal overflow on any of the 14 pages at
390px, and all 38 reveal buttons toggle.

**Session 7's timings are machine specific** and will not match the classroom machines.
The shape holds, which is what the session is about. Re-run before teaching it.

## Standards

Mapped to the Common Core State Standards for Mathematics (Multi-State). All 26 codes and
their wording were retrieved from the CASE Network through the Learning Commons Knowledge
Graph on 12 September 2026. At most three per session. Footers say "mapped to".

The graph carries Common Core and state-adopted frameworks. It returned nothing for New
York's Computer Science and Digital Fluency codes in any format tried, and it rejects
CSTA as a jurisdiction outright, naming `Multi-State` or a US state as the only options.
So **no CSTA or New York code appears anywhere on this site**, because none could be
verified. Paste the codes you want and they can be added under the usual `??` flags.

Three codes fit the material almost exactly. `8.F.A.1` defines a function as a rule
assigning each input exactly one output, which is session 2. `7.SP.C.6` is long-run
relative frequency, which is session 9. `7.SP.C.7` asks students to explain sources of
discrepancy when a model and observed frequencies disagree, which session 9 does with a
real bug supplying the discrepancy.

## Code rules

Version 1, dated 12 September 2026. Published in full on the hub. Summary:

One frame-loop idiom all term: `while running:`, an `event.get()` loop, a QUIT listener,
`clock.tick(60)`. A window never closes itself. Flat gates only. Words in the terminal,
visuals in the window. No `pygame.font`. No bare `except:`. A module never prints. No
turtle, since it does not work in our VS Code setup.

`break` is lifted from session 7 and only where the loop exit is itself the topic. Before
that, every `while` names its exit condition.

Session 1 stays inside the summer vocabulary on purpose so the cold read measures tracing
rather than new syntax.

## Threads that run across sessions

Sessions 1, 2, and 4 use the same ten readings totalling 503. Session 4's dirty file
cleans down to session 1's exact list, which is the retrieval hook.

The strict `>` gate is the spine of the first half. It decides index 5 over index 9 in
session 1, becomes two named functions in session 2, reappears as
`count_over([5,5,5], 5)` returning 0, and returns as the keep rule in session 8.

Every session's teacher panel names the prior concept it re-touches. Seven days between
sessions means every session is a cold start.

## Open decisions

Built one way, can be built the other. Tabled on the hub.

1. **Classes lifted: yes**, from session 12. Flip it and sessions 12 and 13 need
   replacing, since the plume and the orbit both go.
2. **Theme: none.** Sessions 3, 5, and 6 skin cleanly onto Secret Spy Academy. Sessions 7
   to 13 do not.
3. **Capstone sketch due session 10**, the light attendance day. Moving it to session 11
   costs a session of build time.
4. **Cartridge Ledger**: one printed card for the whole term, thirteen rows, stamped at
   the door. Not built here. It is a print artifact, not a page.

## Printing history

First printing, 12 September 2026. Fourteen pages, 26 verified standards, 22 programs.

Known corrections made before first printing: session 9 originally showed the corrected
switch block beside the broken run's numbers, and stated `2/3 = 0.6668` where the real
output says `0.6667`. Both fixed, and `crosscheck.py` now exists to catch that class of
error.
